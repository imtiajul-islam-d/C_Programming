#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    int N;
    scanf("%d", &N);
    int A[N];
    // for(int i = 0; i < N; i++){
    //     scanf("%d", &A[i]);
    //     printf("%d", A[i]);
    //     printf("Hello");
    // }
        printf("%d", N);
    // int X;
    // int V;
    // scanf("%d %d", &X, &V);
    // for(int i = 0; i > N; i++){
    //     if(i == X){
    //         A[i] = V;
    //     }
    // printf("%d", A[i]);
    // }
    
    return 0;
}